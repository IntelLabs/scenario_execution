import osc.standard

action kubernetes_delete:
    target: string
    regex: bool = false # is the target a regular expression
    element_type: string # possible values: pod
    namespace: string = "default"
    grace_period: second = 5s


action kubernetes_create_from_yaml:
    yaml_file: string
    namespace: string = "default"
