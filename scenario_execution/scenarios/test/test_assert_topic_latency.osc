import osc.ros

scenario test_ros_topic_publish:
    do parallel:
        serial:
            assert_topic_latency() with:
                keep(it.topic_name == '/test_topic')
                keep(it.latency == 1.5s)
            emit end