import osc.openrmf
import osc.standard

scenario control_doors:
    do serial:
        door_request() with:
            keep(it.door_ids == "") # To send a request to all doors in the simulation, use an empty string
            keep(it.door_mode == door_mode!open) #open or close
        emit end
        